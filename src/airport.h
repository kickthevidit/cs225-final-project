#pragma once

#include <string>
#include <map>

class Airport {
public:
    std::string print();
private:
    // int id;
    std::string name = "fuck";
    // std::string iata;
    // double latitude;
    // double longitude;
    // std::map<int, int> dest; 
};